name: Build malidp_drv Module Only

on:
  workflow_dispatch:

jobs:
  build_module:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout kernel source
      uses: actions/checkout@v4
      with:
        repository: abdulla-lin/android_kernel_6.1_nothing_mt6878
        ref: main

    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          bc bison build-essential cpio curl flex git libssl-dev \
          libncurses5-dev zlib1g-dev libelf-dev dwarves \
          wget unzip xz-utils tar clang lld llvm binutils

    - name: Setup LLVM toolchain
      run: |
        export PATH=$GITHUB_WORKSPACE/prebuilts/clang/host/linux-x86/clang-17.0.2/bin:$PATH
        export ARCH=arm64
        export SUBARCH=arm64
        export CC=clang
        export LLVM=1
        export LLVM_IAS=1
        echo "PATH=$PATH" >> $GITHUB_ENV
        echo "ARCH=arm64" >> $GITHUB_ENV
        echo "SUBARCH=arm64" >> $GITHUB_ENV
        echo "CC=clang" >> $GITHUB_ENV
        echo "LLVM=1" >> $GITHUB_ENV
        echo "LLVM_IAS=1" >> $GITHUB_ENV

    - name: Prepare kernel for module build
      run: |
        export O=out
        export ARCH=arm64
        export SUBARCH=arm64
        make O=$O gki_defconfig
        make O=$O olddefconfig
        make O=$O prepare
        make O=$O modules_prepare

    - name: Build malidp_drv module
      run: |
        export O=out
        cd drivers/gpu/drm/arm
        make -C $GITHUB_WORKSPACE O=$O M=$(pwd) modules

    - name: Copy malidp_drv.ko
      run: cp out/drivers/gpu/drm/arm/malidp_drv.ko $GITHUB_WORKSPACE/

    - name: Upload malidp_drv module
      uses: actions/upload-artifact@v4
      with:
        name: malidp_drv.ko
        path: malidp_drv.ko
        
